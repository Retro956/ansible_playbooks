- name: Make Directory at Specified Location
  hosts: ansible_hosts

  tasks:
    - name: Creating a Diretory (~/testing)
      ansible.builtin.file:
        path: ~/git
        state: directory
        mode: '0755'
